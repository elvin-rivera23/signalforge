# Reserved for future config; never commit real secrets.
# Copy to .env and edit locally when needed.

# Data client config (no secrets here)
SF_PROVIDER=YAHOO_CHART         # simple, no key; may be rate-limited
SF_HTTP_TIMEOUT_S=4             # upstream timeout
SF_CACHE_TTL_S=15               # short TTL (near-realtime feel)
SF_MAX_CACHE_SIZE=256           # LRU-ish cap
